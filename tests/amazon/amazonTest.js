describe("test suite: integration test for amazon page", () => {
  it("displays products on the page", () => {});
  it("addToCart button works add item to the cart and updates cartQuantity", () => {});
});
